// import { mockDBSales } from "@/app/(app-ui)/sales/history/[client_id]/[sales_id]/components/mockData";
// import { GetSalesOut } from "@/types/Sales";

// export async function fetchSalesData(sales_id: number): Promise<GetSalesOut | undefined> {
//     // 今はモックで代用
//     const data = mockDBSales.find((s) => s.sales_id === sales_id);

//     // 実際にはAPIやDBから取得する想定
//     // const res = await fetch(`${process.env.API_URL}/sales/${sales_id}`);
//     // const data = await res.json();

//     return data;
// }
